# Public signing baseline

This marker triggers and records validation of the persistent public signing baseline. The first public install may require uninstalling the legacy private-build app; subsequent public releases use the same public signing identity and support normal in-place updates.
